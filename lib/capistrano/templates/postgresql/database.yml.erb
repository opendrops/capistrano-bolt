<%= fetch(:stage) %>:
  adapter: postgresql
  timeout: 5000
  encoding: utf8
  reconnect: false
  database: <%= "#{fetch(:postgresql_user)}" %>
  pool: 5
  username: <%= "#{fetch(:postgresql_database)}" %>
  host: <%= "#{fetch(:postgresql_host)}" %>
  port: 5432
